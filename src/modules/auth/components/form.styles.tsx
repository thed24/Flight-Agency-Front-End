import { styled } from '@mui/material/styles';

export const FormContainer = styled('form')`
    display: flex;
    flex-direction: column;
    gap: 1rem;
    padding: 1rem;
    max-width: 400px;
    margin: auto;
`;
